export type Galas ={
    id: number,
    day: string,
    date: Date,
    quantity: number,
    nominated: number,
    totalNominated: number,
    galaTypeId: number,
    stateId:number,
    weekId:number,
    modal:string | null
}
